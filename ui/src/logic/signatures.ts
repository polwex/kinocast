export function hashAndSign() {
  const hashScheme = "HASH_SCHEME_BLAKE3";
  const signatureScheme = "SIGNATURE_SCHEME_ED25519";
  const hash = "";
  const signer = "";
  const signature = "";
  return { hash, hashScheme, signature, signatureScheme, signer };
}
